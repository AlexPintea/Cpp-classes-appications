size is a string.
common size and string.
common size string is an and size string.
is an and size string.
common size string is.
files are a lot.
a file is a lot.
files have size.
size have a lot and is files.
